Counter2 fast shortcut

Use this for daily billing:
OPEN_COUNTER2_FAST.bat

Or use:
Badizo Counter2 Fast.url

URL:
http://192.168.1.9:5000?loginMode=counter&loginUser=counter2

The server now sends no-cache headers, so this fast shortcut is safe.
Do not use old Badizo app icons or any shortcut that opens port 3000.

Use the older RESET_COUNTER2_BROWSER_CACHE.bat only if the browser behaves strangely.
